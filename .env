# n8n AI Assistant - template only. Replace CHANGE_ME values before deployment.
# Do NOT commit real passwords/API keys to a public repository.

N8N_VERSION=2.36.9

# Domains (replace later)
N8N_HOST=n8n.example.com
SEARXNG_HOST=search.example.com

# Existing Traefik installation
TRAEFIK_NETWORK=traefik_proxy
TRAEFIK_HTTPS_ENTRYPOINT=websecure
TRAEFIK_CERTRESOLVER=letsencrypt

# Locale
GENERIC_TIMEZONE=Europe/London

# PostgreSQL
POSTGRES_DB=n8n
POSTGRES_USER=n8n
POSTGRES_PASSWORD=CHANGE_ME_STRONG_POSTGRES_PASSWORD

# n8n security
N8N_ENCRYPTION_KEY=CHANGE_ME_RANDOM_64_CHAR_ENCRYPTION_KEY
N8N_RUNNERS_AUTH_TOKEN=CHANGE_ME_RANDOM_RUNNER_TOKEN

# SearXNG
SEARXNG_SECRET=CHANGE_ME_RANDOM_SEARXNG_SECRET

# AI provider - add when ready
OPENAI_API_KEY=CHANGE_ME_LATER
